# compose.reg.yml 用のデータベース接続情報。
# このファイルを .env にコピーして、node-02〜05 に配置する。
# 値は infra/terraform の出力・変数と揃える。

# terraform output db_host
DB_HOST=192.168.1.30

# var.db_username (同名のデータベースが作成される)
DB_NAME=sakuravel_app
DB_USER=sakuravel_app

# var.db_password (1Password の DB パスワード)
DB_PASSWORD=
