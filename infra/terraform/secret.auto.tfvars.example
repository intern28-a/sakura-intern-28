# 最初にこのファイルをコピーして編集します
# cp secret.auto.tfvars.example secret.auto.tfvars

# さくらのクラウド API トークン
sakura_access_token = "YOUR_SAKURA_ACCESS_TOKEN"

# さくらのクラウド API トークンシークレット
sakura_access_token_secret = "YOUR_SAKURA_ACCESS_TOKEN_SECRET"

# データベースのパスワード(データベース作成とデータベース接続時に使用)
# 1PasswordのDBパスワードからコピーします
db_password = "YOUR_DB_PASSWORD"

# VMのパスワード(ログインやsudoコマンド実行時に使用)
# 1PasswordのVMパスワードからコピーします
server_password = "YOUR_SERVER_PASSWORD"

# SSH 公開鍵のパス
# 空のままだとサーバーに公開鍵が一切インストールされず、パスワード認証だけになります。
# NOTE: この値は cloud-init 経由で反映されるため、初回 apply(サーバー作成時)にしか効きません。
#       構築後に変更した場合は ssh-copy-id で手動配布してください(infra/README.md 参照)。
server_ssh_public_key_path = "~/.ssh/id_ed25519.pub"
